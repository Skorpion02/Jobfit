# === Dependencias para la imagen Docker (GPU + OCR) ===
# NOTA: torch NO se incluye aquí; se instala aparte en el Dockerfile como
# wheel CUDA (pip install torch --index-url https://download.pytorch.org/whl/cu124).

# === CORE ===
gradio>=4.4.0
sentence-transformers>=3.0.0
scikit-learn>=1.3.0
numpy>=1.24.0

# === WEB SCRAPING ===
requests>=2.31.0
beautifulsoup4>=4.12.0

# === DOCUMENT PROCESSING ===
python-docx>=1.1.0
PyPDF2>=3.0.0
pdfplumber>=0.10.0
pymupdf>=1.24.0
pdf2image>=1.17.0

# === OCR para PDFs escaneados (habilitado en la imagen Docker) ===
# Requiere el binario de sistema poppler-utils (instalado en el Dockerfile).
easyocr>=1.7.0

# === LM STUDIO CLIENT (OpenAI Compatible API) ===
openai>=1.0.0

# === DATA & UTILITIES ===
pydantic>=2.4.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
psutil>=5.9.0

# === DEV / TESTING ===
pytest>=7.0.0
